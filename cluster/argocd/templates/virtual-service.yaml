{{- if .Values.istio.enabled -}}
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ include "argo-cd.name" . }}-vs
  namespace: {{ .Values.istio.namespace | default "argocd" }}
spec:
  hosts:
  - {{ .Values.istio.host | default "argocd.local" }}
  gateways:
  - {{ .Values.istio.gateway | default "istio-system/gateway" }}
  http:
  - route:
    - destination:
        host: {{ include "argo-cd.name" . }}.{{ .Values.istio.namespace | default "argocd" }}.svc.cluster.local
        port:
          number: 80
{{- end -}}
