apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudnative-pg
  namespace: argocd
spec:
  project: default
  source: 
    repoURL: https://github.com/noh4nsen/lab.git
    targetRevision: HEAD
    path: cluster/bootstrap/cloudnative-pg
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cnpg
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
